server:
  port: 8081

spring:
  application:
    name: appointment-service

  datasource:
    url: jdbc:postgresql://localhost:5432/appointment_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

clients:
  doctor-service:
    base-url: http://localhost:8082
  patient-service:
    base-url: http://localhost:8084
  notification-service:
    base-url: http://localhost:8083
    appointment-events-path: /appointment-events

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: health,info